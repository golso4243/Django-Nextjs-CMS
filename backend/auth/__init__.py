"""
Lightweight module for authentication-related API endpoints.

This is intentionally minimal for now and provides only placeholder/stub
views under the /api/auth/ namespace so that real authentication logic
can be added later (JWT or session-based) without changing the public
blog API.
"""

